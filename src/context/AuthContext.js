import React, {createContext, useState} from 'react';


export const authContext = createContext()

const AuthContextProvider = () => {
    const verificationMode = useState()


    return (
        <div>

        </div>
    );
};

export default AuthContextProvider;